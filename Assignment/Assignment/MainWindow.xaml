<Window x:Class="Assignment.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Assignment"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="740">
    <Grid>
        <DataGrid x:Name="CoinsGrid" ItemsSource="{Binding CoinsList}" Width="545" 
                  Margin="185,50,10,10" AutoGenerateColumns="False" CanUserAddRows="False"
                  CanUserDeleteRows="False" CanUserResizeRows="False"> 
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Width="100" Binding="{Binding Path=Name}"/>
                <DataGridTextColumn Header="Symbol" Width="50" Binding="{Binding Path=symbol}" />
                <DataGridTextColumn Header="Price" Width="100" Binding="{Binding Path=current_price}"/>
                <DataGridTextColumn Header="24h" Width="70" Binding="{Binding Path=price_change_percentage_24h}"/>
                <DataGridTextColumn Header="Capitalization" Width="100" Binding="{Binding Path=market_cap}"/>
                <DataGridTextColumn Header="Total Volume" Width="100" Binding="{Binding Path=total_volume}"/>
                
            </DataGrid.Columns>
            </DataGrid>
    </Grid>
</Window>
